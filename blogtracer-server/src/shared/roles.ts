enum Roles {
    USER = 'role.user',
    CONTENT_CREATOR = 'role.creator',
    GUEST = 'role.guest',   
    MODERATOR = 'role.mod',
    ADMIN = 'role.admin',
    NONE = 'role.none'
}

export default Roles;